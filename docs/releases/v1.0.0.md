# v1.0.0

Initial public release.

## Summary

This release packages Codex Thread Extractor as a Codex skill and plugin for
local Codex Desktop thread extraction.

## Verification

```text
python scripts\verify_plugin.py

Plugin validation passed
Skill metadata valid
Python compilation passed
4 extractor tests passed
```

## Public Safety Check

The public repository was scanned for local absolute paths and common secret
patterns before tagging. Private local notes remain under `.private/`, which is
ignored by git.

## Git

Release tag: `v1.0.0`
