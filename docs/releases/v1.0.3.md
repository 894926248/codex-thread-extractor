# v1.0.3

Automatic route by need.

## Summary

This release teaches the skill to route different old-thread requests to the
right path: native `read_thread` for quick recent-state checks, and
`codex-thread-extractor` for continue-work, recovery, long-thread, and
damaged-thread scenarios.

It also tightens clue-based `--find` ranking so direct thread hits stay ahead
of audit or tool-meta threads that merely repeat the same clue text.

## Verification

```text
python scripts\verify_plugin.py
```

## Git

Release tag: `v1.0.3`
